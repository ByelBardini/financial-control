# Dependências da raiz (concurrently)
node_modules/

# Variáveis de ambiente locais
.env

# Logs
*.log

# Artefatos de build / cache do Expo (client)
client/dist/
client/web-build/
.expo/

# Locks do harness do Claude (não versionar)
.claude/*.lock
